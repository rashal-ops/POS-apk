name: pos_app
description: "A complete offline POS system for Android"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  path_provider: ^2.1.2
  permission_handler: ^11.0.0
  blue_thermal_printer: ^1.2.3
  excel: ^4.0.3
  pdf: ^3.10.7
  printing: ^5.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
